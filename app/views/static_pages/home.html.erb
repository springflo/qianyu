<% provide(:title,"Hi~ o(*￣▽￣*)ブ") %>

<% if logged_in? %>
  <div class="row">
      <aside class="col-md-4">
          <section class="user_info">
              <%= render 'shared/user_info' %>
          </section>
          
          <section class="stats">
              <%= render 'shared/stats'%>
          </section>
          
          <section class="post_form">
              <%= render 'shared/post_form' %>
          </section>
      </aside>
      <div class="col-md-8">
          <h3>千人千语</h3>
          <%= render 'shared/feed' %>
      </div>
  </div>

<% else %>
    <div class="center jumbotron" >
        <h1>欢迎来到千语！</h1>
        <h2>
            <!--This is the home page for the-->
            <!--<a href="http://www.railstutorial.org/">Ruby on Rails Tutorial</a>-->
            <!--sample application.-->
            <%= link_to image_tag("doggy.png", alt: "欢迎", size: 50) %>
        </h2>
        <%= link_to "注册新用户", signup_path, class: "btn btn-lg btn-primary" %>
    </div>
<% end %>

